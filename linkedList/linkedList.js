const listFactory = () => {
  const head = null;
  const tail = null;

  return { head, tail };
};

function createNode() {
  return {
    nextNode: null,
    value() {
      return null;
    },
  };
}
